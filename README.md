# scim-proxy

A simple, bun based, scim-proxy to block reads, writes or just juggle around data.

Use docker compose to deploy.

See scim-proxy.toml on how to configure.

## Development

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```
